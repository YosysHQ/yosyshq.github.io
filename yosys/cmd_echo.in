@cmd_header echo@
<h1>echo - turning echoing back of commands on and off</h1>
<pre>
    echo on

Print all commands to log before executing them.


    echo off

Do not print all commands to log before executing them. (default)

</pre>
@footer@
