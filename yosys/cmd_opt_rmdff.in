@cmd_header opt_rmdff@
<h1>opt_rmdff - remove DFFs with constant inputs</h1>
<pre>
    opt_rmdff [-keepdc] [selection]

This pass identifies flip-flops with constant inputs and replaces them with
a constant driver.

</pre>
@footer@
