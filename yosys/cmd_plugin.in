@cmd_header plugin@
<h1>plugin - load and list loaded plugins</h1>
<pre>
    plugin [options]

Load and list loaded plugins.

    -i &lt;plugin_filename&gt;
        Load (install) the specified plugin.

    -a &lt;alias_name&gt;
        Register the specified alias name for the loaded plugin

    -l
        List loaded plugins

</pre>
@footer@
