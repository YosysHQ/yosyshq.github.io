@cmd_header sta@
<h1>sta - perform static timing analysis</h1>
<pre>
    sta [options] [selection]

This command performs static timing analysis on the design. (Only considers
paths within a single module, so the design must be flattened.)

</pre>
@footer@
